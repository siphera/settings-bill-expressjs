<div class="row">
    <h1>actions</h1>

    <table>
        <tr>
            <th>Type</th>
            <th>Price</th>
            <th>Timestamp</th>
        </tr>
        {{#each actions}}
        <tr>
            <td>{{type}}</td>
            <td>R{{cost}}</td>
            <td>{{timestamp}}</td>
        </tr>
        {{/each}}
    </table>
</div>